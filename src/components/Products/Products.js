import React from "react";
import Layout from "../Layout/Layout";

const Products = (props) => {
  return (
    <div>
      <Layout sidebar>
        <h1>Hi</h1>
      </Layout>
    </div>
  );
};

export default Products;
